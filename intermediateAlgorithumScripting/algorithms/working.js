function convertHTML(str) {
  // &colon;&rpar;

    var output = str.match(/\&/);
    console.log(output);

    //have index of symbol
    //need to replace symbol with specific string.

    var regEx = /\&/;

    str.replace(regEx, )




  // return str;
}

convertHTML("Dolce & Gabbana");
